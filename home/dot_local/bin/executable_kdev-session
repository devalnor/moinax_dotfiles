#!/bin/bash
# Emit kitty session config for the kdev/wts dev layout.
# Usage: kdev-session [extra-claude-args...]
cat <<EOF
new_tab Claude
layout splits
launch --title kdev-claude claude $@
launch --title kdev-diff --location=vsplit $HOME/.local/bin/kdev-watch-diff.sh
focus_matching_window title:kdev-claude
launch --title kdev-term --location=hsplit
focus_matching_window title:kdev-claude

new_tab Neovim
launch nvim
EOF
